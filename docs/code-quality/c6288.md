---
description: 'Дополнительные сведения о: C6288'
title: C6288
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6288
helpviewer_keywords:
- C6288
ms.assetid: 3856b80a-c9f2-4e86-97fc-c913b0186788
ms.openlocfilehash: 083768d08106580edf0e7a739b05862204a911ae
ms.sourcegitcommit: d6af41e42699628c3e2e6063ec7b03931a49a098
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 12/11/2020
ms.locfileid: "97178346"
---
# <a name="c6288"></a>C6288

> предупреждение C6288: неверный оператор: взаимное включение по && всегда равно нулю. Вы предполагали использовать вместо этого &#124;&#124; ?

Это предупреждение означает, что в тестовом выражении переменная проверяется на соответствие двум различным константам, и результат зависит от обоих условий. Код в этих случаях указывает, что намерение программиста не захвачено правильно. Важно изучить код и устранить проблему; в противном случае код будет работать не так, как ожидалось.

Как правило, эта проблема вызвана использованием `&&` ; вместо `||` , но также может быть вызвана использованием `==` Where `!=` .

## <a name="example"></a>Пример

Это предупреждение создается в следующем коде:

```cpp
void f(int x)
{
  if ((x == 1) && (x == 2)) // warning
  {
    // code ...
  }
}
```

Чтобы исправить это предупреждение, используйте следующий код:

```cpp
void f(int x)
{
     if ((x == 1) || (x == 2))
     {
          // logic
     }

    /* or */
    if ((x != 1) && (x != 2))
    {
        // code ...
    }
}
```

Средство анализа не предупреждает, если выражение имеет побочные эффекты.
